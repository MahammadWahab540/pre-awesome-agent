const nextConfig = {
    // Removed output: 'export' to enable server-side rendering for dynamic routes
    images: {
        unoptimized: true,
    },
    trailingSlash: true,
};

module.exports = nextConfig;
